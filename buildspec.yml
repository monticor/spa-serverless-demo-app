version: 0.2
phases:
  install:
      commands:
        - npm install -g serverless
  build:
     commands:
          - cd serverless-cognito-auth/
          - serverless deploy --stage dev us-east-1 --verbose
